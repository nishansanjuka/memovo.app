# =============================================================================
# MEMOVO.APP - CI/CD Environment Variables
# =============================================================================
# This file documents all environment variables needed for CI/CD pipelines
# Configure these as Jenkins Credentials or Secret Manager entries
# =============================================================================

# -----------------------------------------------------------------------------
# Google Cloud Platform (Required for Deployment)
# -----------------------------------------------------------------------------

# GCP Project ID - Jenkins Credential ID: gcp-project-id
GCP_PROJECT_ID=your-gcp-project-id

# GCP Region for Cloud Run deployment
GCP_REGION=us-central1

# Google Artifact Registry path
ARTIFACT_REGISTRY=us-central1-docker.pkg.dev/your-gcp-project-id/memovo

# GCP Service Account Key (JSON) - Jenkins Credential ID: gcp-service-account-key
# This is a file credential, not set as environment variable

# -----------------------------------------------------------------------------
# Turborepo Remote Cache (Optional but Recommended)
# -----------------------------------------------------------------------------

# Turborepo Team Name - Jenkins Credential ID: turbo-team
TURBO_TEAM=your-turbo-team

# Turborepo Token - Jenkins Credential ID: turbo-token
TURBO_TOKEN=your-turbo-token

# -----------------------------------------------------------------------------
# Build Information (Auto-set by Pipeline)
# -----------------------------------------------------------------------------

# Git commit SHA (first 8 characters)
BUILD_TAG=auto-generated

# Full Git commit SHA
GIT_COMMIT=auto-generated

# -----------------------------------------------------------------------------
# Application Secrets (Cloud Run Environment Variables)
# -----------------------------------------------------------------------------
# These should be configured directly in Cloud Run services or via Secret Manager

# --- API Service (Java) ---
# SPRING_PROFILES_ACTIVE=production
# DATABASE_URL=postgresql://...
# CLERK_SECRET_KEY=sk_live_...

# --- Gateway Service (NestJS) ---
# NODE_ENV=production
# CLERK_PUBLISHABLE_KEY=pk_live_...
# CLERK_SECRET_KEY=sk_live_...

# --- LLM Service (Python) ---
# ENVIRONMENT=production
# GOOGLE_API_KEY=...
# PINECONE_API_KEY=...
# MONGODB_URI=mongodb+srv://...

# --- Web App (Next.js) ---
# NEXT_PUBLIC_API_URL=https://api.memovo.app
# NEXT_PUBLIC_GATEWAY_URL=https://gateway.memovo.app
